{
  "name": "other_chain_user",
  "version": "1.0.0",
  "description": "Simulates a blockchain user and is only required to create transactions that can be indexed by the blockchain-indexer.   This is necessary because the blockchain-indexer only emits events when it indexed a transaction.   If this container is not running, then some processes like the pathfinder-updater will report an unhealthy status.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.11.18",
    "@types/web3": "^1.2.2",
    "typescript": "^4.9.4",
    "web3": "^1.8.1"
  }
}
